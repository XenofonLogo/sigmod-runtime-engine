Εργασία Ανάπτυξη Λογισμικού για Πληροφοριακά Συστήματα (1ο μέρος) 

Ονοματεπωνυμο 
Ξενοφών Λογοθέτης 1115202100087
Ονοματεπωνυμο 




αλγόριθμος κατακερματισμού robin hood
_____________________________________

υλοποιήθηκε από 

...



αλγόριθμος κατακερματισμού hopscotch
_____________________________________


υλοποιήθηκε από Ξενοφών Λογοθέτης

Αντικατάσταση std::unordered_map με tsl::hopscotch_map

Η βασική αλλαγή αφορά τη δομή του hash table που χρησιμοποιείται
στον αλγόριθμο join.

Αντί για `std::unordered_map`, χρησιμοποιείται πλέον:
    `tsl::hopscotch_map`


Προσθήκη βοηθητικής συνάρτησης try_normalize

Προστέθηκε η inline συνάρτηση `try_normalize`, η οποία επιχειρεί
να "ομαλοποιήσει" το κλειδί (join key) σε ενιαίο τύπο T.

Λειτουργία:
- Επιστρέφει `std::optional<T>`, το οποίο είναι `nullopt`
  αν το key δεν μπορεί να μετατραπεί με ασφάλεια.
- Υποστηρίζει:
    • Μετατροπή αριθμητικών τύπων (int, double)
    • Μετατροπή αριθμών σε string (αν ο join key είναι VARCHAR)
    • Αγνόηση `std::monostate` (NULL τιμές)

Στόχος:
- Αποφυγή exceptions κατά τη σύγκριση διαφορετικών τύπων.
- Καλύτερη ανθεκτικότητα σε heterogenous data sets.
- Επέκταση συμβατότητας join πεδίων διαφορετικού τύπου.

Αφαίρεση ρίψης exceptions για type mismatch

Στην αρχική έκδοση, κάθε ασυμβατότητα τύπου προκαλούσε
`throw std::runtime_error("wrong type of field")`.

Πλέον:
- Οι ασύμβατες ή null τιμές αγνοούνται απλά.
- Το join συνεχίζει κανονικά χωρίς διακοπή.
- Έτσι βελτιώνεται η σταθερότητα σε mixed ή ελλιπή δεδομένα.

Βελτιώσεις στην Build / Probe φάση

Η βασική λογική παρέμεινε ίδια, αλλά:
- Οι φάσεις build και probe χρησιμοποιούν τη normalize συνάρτηση.
- Οι κλειδιά αποθηκεύονται μόνο αν έχουν έγκυρη τιμή (όχι monostate).
- Ελαχιστοποιήθηκε ο επαναλαμβανόμενος κώδικας και ο έλεγχος τύπων.

 Καμία αλλαγή στη λογική του Join ή στα δεδομένα εξόδου


Συνολικός χρόνος εκτέλσης των queries: 737201 ms (προηγουμένως 493646)


αλγόριθμος cuckoo hashing
_________________________

υλοποιήθηκε από 

...

